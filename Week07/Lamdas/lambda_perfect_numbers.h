#ifndef __LAMBDA_PERFECT_NUMBERS_H
#define __LAMBDA_PERFECT_NUMBERS_H

#include <vector>
using namespace std;

bool perfect_number(const int& x);
vector<int> perfects_numbers_in_range(const int& from, const int& to);

#endif